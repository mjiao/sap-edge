# SPDX-FileCopyrightText: 2025 SAP edge team
# SPDX-FileContributor: Manjun Jiao (@mjiao)
#
# SPDX-License-Identifier: Apache-2.0

---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: rosa-teardown
spec:
  description: Teardown ROSA cluster and AWS services using Terraform destroy
  params:
    - name: clusterName
      type: string
      description: "ROSA cluster name"
    - name: awsSecretName
      type: string
      description: "Name of the Kubernetes Secret containing AWS credentials"
      default: "aws-credentials-secret"
    - name: redhatTokenSecretName
      type: string
      description: "Name of the Kubernetes Secret containing Red Hat OCM token"
      default: "redhat-token-secret"
  workspaces:
    - name: source
  steps:
    - name: teardown-rosa
      image: registry.access.redhat.com/ubi9/ubi
      timeout: "60m"
      workingDir: $(workspaces.source.path)
      env:
        - name: CLUSTER_NAME
          value: "$(params.clusterName)"
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: $(params.awsSecretName)
              key: AWS_ACCESS_KEY_ID
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: $(params.awsSecretName)
              key: AWS_SECRET_ACCESS_KEY
        - name: REDHAT_OCM_TOKEN
          valueFrom:
            secretKeyRef:
              name: $(params.redhatTokenSecretName)
              key: REDHAT_OCM_TOKEN
      script: |
        #!/usr/bin/env bash
        set -euo pipefail
        
        echo "🗑️ Starting ROSA cluster teardown..."
        echo "====================================="
        echo "Cluster Name: ${CLUSTER_NAME}"
        echo ""
        
        # Install required tools
        echo "📦 Installing required packages..."
        dnf install -y wget unzip jq
        
        # Install Terraform
        echo "📦 Installing Terraform..."
        TERRAFORM_VERSION="1.6.6"
        wget -q "https://releases.hashicorp.com/terraform/${TERRAFORM_VERSION}/terraform_${TERRAFORM_VERSION}_linux_amd64.zip"
        unzip -q "terraform_${TERRAFORM_VERSION}_linux_amd64.zip"
        mv terraform /usr/local/bin/
        terraform version
        
        # Install ROSA CLI
        echo "📦 Installing ROSA CLI..."
        wget -q https://mirror.openshift.com/pub/openshift-v4/clients/rosa/latest/rosa-linux.tar.gz
        tar xzf rosa-linux.tar.gz
        mv rosa /usr/local/bin/
        rosa version
        
        # Install AWS CLI
        echo "📦 Installing AWS CLI..."
        curl -s "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
        unzip -q awscliv2.zip
        ./aws/install
        aws --version
        
        # Verify AWS credentials
        echo "🔐 Verifying AWS credentials..."
        if ! aws sts get-caller-identity; then
          echo "❌ AWS credentials verification failed"
          exit 1
        fi
        
        # Login to Red Hat OCM
        echo "🔐 Logging into Red Hat OpenShift Cluster Manager..."
        rosa login --token="${REDHAT_OCM_TOKEN}"
        
        # Check if cluster exists
        echo "🔍 Checking if ROSA cluster exists..."
        if ! rosa describe cluster -c "${CLUSTER_NAME}" &>/dev/null; then
          echo "✅ No cluster found with name: ${CLUSTER_NAME}"
          echo "Nothing to delete!"
          exit 0
        fi
        
        # Get cluster state
        CLUSTER_STATE=$(rosa describe cluster -c "${CLUSTER_NAME}" -o json | jq -r '.state')
        echo "📋 Current cluster state: ${CLUSTER_STATE}"
        
        # Check if already being deleted
        if [[ "${CLUSTER_STATE}" == "uninstalling" ]]; then
          echo "⏳ Cluster is already being deleted"
          echo "Waiting for deletion to complete..."
          
          # Wait for up to 30 minutes
          for i in {1..60}; do
            if ! rosa describe cluster -c "${CLUSTER_NAME}" &>/dev/null; then
              echo "✅ Cluster successfully deleted"
              exit 0
            fi
            echo "⏳ Still deleting... (${i}/60 checks)"
            sleep 30
          done
          
          echo "⚠️  Cluster deletion is taking longer than expected"
          exit 0
        fi
        
        # Delete the cluster
        echo "🗑️ Deleting ROSA cluster: ${CLUSTER_NAME}"
        echo "⚠️  This will delete:"
        echo "  - ROSA cluster and all workloads"
        echo "  - IAM roles and policies"
        echo "  - VPC and networking (if created by ROSA)"
        echo ""
        
        if rosa delete cluster -c "${CLUSTER_NAME}" --yes; then
          echo "✅ Cluster deletion initiated successfully"
          echo "⏳ Deletion will continue in the background (typically 10-15 minutes)"
        else
          echo "❌ Failed to initiate cluster deletion"
          echo "Manual intervention may be required"
          exit 1
        fi
        
        # Note about Terraform-managed resources
        echo ""
        echo "📝 Note: Terraform-managed AWS resources (PostgreSQL, Redis, S3, etc.)"
        echo "   are NOT automatically deleted by ROSA cluster deletion."
        echo "   Use the cleanup script or Terraform destroy manually if needed:"
        echo "   ./hack/rosa/cleanup-rosa-resources.sh"
        
        # Clean up workspace files
        echo "🧹 Cleaning up workspace files..."
        rm -f rosa-deployment-info.txt
        rm -f cluster-info.json
        rm -f admin-creds.json
        rm -f kubeconfig
        
        echo ""
        echo "✅ ROSA teardown completed!"
        echo "📝 Note: It may take a few minutes for all AWS resources to be fully deleted"

